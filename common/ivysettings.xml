<ivysettings>
	<settings defaultResolver="main"/>
	<include url="${ivy.default.settings.dir}/ivysettings-public.xml"/>
	<include url="${ivy.default.settings.dir}/ivysettings-local.xml"/>
	<resolvers>
		<!-- Copy/paste from ivysettings-local.xml with checkmodified=true set. -->
		<!-- Because we publish snapshots locally, we need to set checkmodified=true to skip the cache. -->
		<filesystem name="local-checkmodified" checkmodified="true">
			<ivy pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}" />
			<artifact pattern="${ivy.local.default.root}/${ivy.local.default.artifact.pattern}" />
		</filesystem>
		<!-- Our local jar cache; eventually will be on the net, I think. -->
		<filesystem name="jars">
			<ivy pattern="${ivy.settings.dir}/../jars/[organisation]/[module]/ivys/ivy-[revision].xml"/>
			<artifact pattern="${ivy.settings.dir}/../jars/[organisation]/[module]/[type]s/[artifact]-[revision].[ext]"/>
		</filesystem>
		<chain name="main" dual="true">
			<!-- Check just-published local before our joist repo. -->
			<resolver ref="local-checkmodified"/>
			<resolver ref="jars"/>
			<resolver ref="public"/>
		</chain>
	</resolvers>
</ivysettings>
